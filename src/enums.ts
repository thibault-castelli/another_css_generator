/**
 * Css properties for demo box.
 */
export enum CssProperty {
   boxShadow,
   textShadow,
   borderRadius,
   border,
   borderDetailed,
   transform,
   color,
   gradient,
   filter,
}
